import 'package:flutter/material.dart';

class TopNavSearch extends StatelessWidget {
  const TopNavSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
