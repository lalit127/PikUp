import 'package:flutter/material.dart';

class TopNavHome extends StatelessWidget {
  const TopNavHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
