import 'package:flutter/material.dart';

class TopNavIcons extends StatelessWidget {
  const TopNavIcons({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
