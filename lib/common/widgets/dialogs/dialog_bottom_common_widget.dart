import 'package:flutter/material.dart';

class DialogBottomCommonWidget extends StatelessWidget {
  const DialogBottomCommonWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
